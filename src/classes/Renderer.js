import P5 from 'p5';

class Renderer {
  constructor ({ containerId, theme }) {
    this.containerId = containerId;
    this.theme = theme;
    this.sketch = null;
  }
  setup () {
  }
}

export default Renderer;
